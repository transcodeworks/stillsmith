---
"@stillsmith/capture": patch
---

`/@fs/` entry URLs are built with forward slashes and a separator after `@fs`, so `stillsmith dev` and `capture` load scene pages on Windows. POSIX output is unchanged.
