<!--
Title format: Conventional Commits, e.g. "fix(capture): await web fonts before the shutter".
Features and API changes need an agreed issue first — see CONTRIBUTING.md.
-->

## What

<!-- What does this PR change, and why? Link the issue if there is one: Fixes #123 -->

## Checklist

- [ ] `pnpm check:fix` then `pnpm check` is clean
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build && pnpm test` passes (tests run against built output)
- [ ] Behavior changes come with a test
- [ ] Public API changes update the docs and package README
