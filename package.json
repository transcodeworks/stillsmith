{
  "name": "stillsmith-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @stillsmith/studio... build",
    "dev": "pnpm --parallel --filter @stillsmith/capture --filter @stillsmith/studio dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @stillsmith/capture --filter @stillsmith/tour --filter @stillsmith/studio test",
    "example": "pnpm --filter stillsmith-example-rock-app",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "docs": "pnpm --filter stillsmith-docs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@changesets/cli": "^2.31.1"
  },
  "packageManager": "pnpm@11.9.0"
}
