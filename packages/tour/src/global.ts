/**
 * IIFE entry. Built as `dist/tour.global.js`, exposing the package as the
 * global `__stillsmithTour` — what stillsmith's MCP `preview_step` injects into a
 * page with `addScriptTag`, the same mechanism as the annotate bundle.
 */
export * from "./index.js";
